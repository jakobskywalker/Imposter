{
  "name": "imposter-game",
  "version": "1.0.0",
  "description": "A real-time multiplayer Imposter game",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "client": "cd client && npm start",
    "build": "cd client && npm install --legacy-peer-deps && CI=false npm run build",
    "heroku-postbuild": "cd client && npm install --legacy-peer-deps && CI=false npm run build",
    "render-postbuild": "cd client && npm install --legacy-peer-deps && CI=false npm run build"
  },
  "keywords": ["game", "multiplayer", "imposter"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.6.1",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
} 